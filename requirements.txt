# Note: Apache Airflow requires Python <3.13
# In production, use Docker with Python 3.11 (see docker-compose.yaml)
# For local development, these are optional dependencies
# If developing scraping DAG locally, use Python 3.11 or 3.12

beautifulsoup4==4.12.2
pymongo==4.6.0
requests==2.31.0

# Production dependencies (installed in Docker)
# apache-airflow==2.10.0
# apache-airflow-providers-mongo==3.1.0